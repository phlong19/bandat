import { useGetREPostData } from "./useGetREPostData";

function PostBarChart() {
  // const { data, count, isLoading } = useGetREPostData();

  return <div>ben nay 1 cai chart nao day, kh can count</div>;
}

export default PostBarChart;
